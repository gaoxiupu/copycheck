
Prompt：
## v1

请分析以下网页文本内容，检查以下方面：
1. 拼写错误（如单词的单词拼写错误：accessory拼写成accesory）
2. 排版错误（如英文数字混合排版时的空格使用不规范，5 V 写为5V）
3. 一致性问题（同一表达在同一页面有多种形式，如混用公制和英制单位）
4. 其他（其他影响用户体验的**文案**方面的问题）


请返回JSON格式的检查结果。确保返回的是一个合法的JSON对象数组，每个对象必须包含以下字段：
- "type": 问题类型 ，必须是"拼写错误", "排版错误", "一致性问题"或“其他”中的一个。
- "severity": 问题的严重程度，必须是 "严重", "中等", 或 "轻微" 中的一个。
- "description": 对问题的具体描述。
- "location": 问题在页面中的位置信息 (例如相关的DOM元素或文本片段)。
- "suggestion": 建议的修改方案。

例如: [{"type": "拼写错误", "severity": "中等", "description": "'登入' 可能是 '登录' 的误用。", "location": "header.login-button", "suggestion": "将 '登入' 修改为 '登录'"}]`;

## v2
请分析以下网页文本内容，并检查以下方面：

1. 拼写错误（如单词拼错：accessory 错误拼写成 accesory）
2. 排版错误（如中英文、数字混排时的空格与符号不规范： 5V 应写为 5 V）
3. 一致性问题（同一表达在同一页面有多种形式，如混用公制和英制单位）
4. 其他（仅限文案表达问题，例如歧义、表述不明确等）

请返回JSON格式的检查结果。要求：
- 输出必须是一个合法的JSON对象数组，不得包含额外文字。
- 每个对象必须包含以下字段：
  - "type": "拼写错误" | "排版错误" | "一致性问题" | "其他"
  - "severity": "严重" | "中等" | "轻微"
  - "description": 对问题的具体描述
  - "location": 问题出现的位置（如DOM选择器；若无DOM信息，请用原始文本片段）
  - "suggestion": 修改或优化建议

请尽量覆盖所有发现的问题。

示例:
[
  {
    "type": "拼写错误",
    "severity": "中等",
    "description": "'登入' 可能是不规范用法，应为 '登录'",
    "location": "header.login-button",
    "suggestion": "将 '登入' 修改为 '登录'"
  }
]
